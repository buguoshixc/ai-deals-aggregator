# mockups/v4 —— 方案 C「首页按意图重排」（已暂缓）

**状态：2026-09-23 按用户决定撤下。页面文件已删除，只保留生成器。**

用户的决定：**不额外加一个主页**；入口的级别 / 分层问题先不议。

两点如实说明：

- **线上 `index.html` 从未为此改动过一行**。C2 自始至终只产出评审用的静态文件（`mockups/v4/intent/`），
  所以撤下评审件不涉及任何站点回退。
- 撤下的是**页面**，不是结论。生成器仍在，因为重做时不该重新发明口径（见下）。

## 想恢复评审页（一条命令）

```bash
npm run build                                    # 页面从真实产物衍生，产物必须先存在
node mockups/v4/_tools/build-intent.js --in=dist # → mockups/v4/intent/{index,deals,free,edu,cn,tools}.html
node scripts/serve.js --port 8081                # 开 http://127.0.0.1:8081/mockups/v4/intent/index.html
```

`:8081` 必须服务**仓库根目录**（`serve.js` 的默认行为），页面里的 `../../../dist/logos.css`
与 `dist/deal/…` 链接才解析得到；直接双击文件也可以，但详情页链接需要先 `npm run build`。

## 生成器口径（重做时照这个来）

`_tools/build-intent.js` **不重画卡片**：它复用产物的 `<style>`、页头、页脚与卡片的原始 HTML，
只替换「入口层」，所以看到的长相就是线上长相。计数全部现场数出，写入 `_summary.json`
（当时：全部优惠 62 / 免费额度 38（完全免费 12 + 免费额度 26）/ 学生·教育 13（身份优惠共 19）/
国内厂商 42 / 全部工具 112 / 已核验 32）。**注意计数会随数据变**：`origin/master` 上已有定时 CI
的数据提交（详见 `PROJECT_STATUS.md` 2.23 末段），并进来之后要重新生成。

## 已经踩过的两个坑（重做时会再遇到）

1. **不能整段搬运产物的 `<head>`**：它已经含主题脚本、`<style>` 与 5 段 JSON-LD，搬完再追加一份
   就会重复注入脚本与 CSS，还会把真站的结构化数据带进 mockup。要**显式重建** head。
2. **必须删掉主应用脚本**：否则它会重新渲染网格，把筛好的静态集合冲掉；第一段主题预涂脚本要留，
   否则暗色不跟随系统。
